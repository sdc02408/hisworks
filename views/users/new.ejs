<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
    <link href="/css/register.css" rel="stylesheet">
  </head>
  <body>
    <%- include('../partials/mynav') %>


      <form action="/users" method="post" id="new-form">


            <!--Grid column-->
            <div class="col-md-6 col-xl-5 card_box">
              <!--Form-->
              <div class="card " data-wow-delay="0.3s">
                <div class="card-body">
                  <!--Header-->
                  <div class="text-center">
                    <div class="choose-login">
                      <a href="/login" class="card-select">Log In</a>
                      <a href="/users/new" class=" card-select">Sign Up</a>
                      <hr class="hr-light">
                    </div>
                  </div>

        <div class="form-group row">
          <label for="username" class="col-sm-3 col-form-label">ID*</label>
          <div class="col-sm-9">
            <input type="text" id="username" name="username" value="<%= user.username %>" class="form-control <%= (errors.username)?'is-invalid':'' %>">
            <% if(errors.username){ %>
              <span class="invalid-feedback"><%= errors.username.message %></span>
            <% } %>
          </div>
        </div>
        <div class="form-group row">
          <label for="name" class="col-sm-3 col-form-label">Name*</label>
          <div class="col-sm-9">
            <input type="text" id="name" name="name" value="<%= user.name %>" class="form-control <%= (errors.name)?'is-invalid':'' %>">
            <% if(errors.name){ %>
              <span class="invalid-feedback"><%= errors.name.message %></span>
            <% } %>
          </div>
        </div>
        <div class="form-group row">
          <label for="email" class="col-sm-3 col-form-label">Email*</label>
          <div class="col-sm-9">
            <input type="text" id="email" name="email" value="<%= user.email %>" class="form-control <%= (errors.email)?'is-invalid':'' %>">
            <% if(errors.email){ %>
              <span class="invalid-feedback"><%= errors.email.message %></span>
            <% } %>
          </div>
        </div>
        <div class="form-group row">
          <label for="password" class="col-sm-3 col-form-label">Password*</label>
          <div class="col-sm-9">
            <input type="password" id="password" name="password" value="" class="form-control <%= (errors.password)?'is-invalid':'' %>">
            <% if(errors.password){ %>
              <span class="invalid-feedback"><%= errors.password.message %></span>
            <% } %>
          </div>
        </div>
        <div class="form-group row">
          <label for="passwordConfirmation" class="col-sm-3 col-form-label">Password*</label>
          <div class="col-sm-9 col-sm-offset-3">
            <input type="password" id="passwordConfirmation" name="passwordConfirmation" value="" class="form-control <%= (errors.passwordConfirmation)?'is-invalid':'' %>">
            <% if(errors.passwordConfirmation){ %>
              <span class="invalid-feedback"><%= errors.passwordConfirmation.message %></span>
            <% } %>
          </div>
        </div>
        <p>
          <small>*Essentials</small>
        </p>

        <% if(errors.unhandled){ %>
          <div class="alert alert-danger">
            <%= errors.unhandled %>
          </div>
        <% } %>

        <div class="submitbtn">
          <button type="submit" class="btn btn-dark col-4">Submit</button>
        </div>
                  <hr class="hr-light mb-3 mt-4">
<!--                  <div class="agree">계정 신청을 하면 <a data-target="#agree-form" data-toggle="modal"><span>이용 약관 및 개인정보 보호 정책</span></a>에 동의한 것으로 간주됩니다.</div>-->
<!--                </div>-->
              </div>
            </div>
          </div>
        </div>
      </form>


    <!-- Central Modal Small -->
    <div class="modal fade" id="agree-form" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
         aria-hidden="true">

      <!-- Change class .modal-sm to change the size of the modal -->
      <div class="modal-dialog col-lg-12 modal-no-padding" role="document">


        <div class="modal-content photo-modal">
          <div class="modal-header">
            <h5 class="modal-title w-100" id="myModalLabel">저작권</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          저작권은 양재명 사진작가에게 있습니다.
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary btn-sm" id="saveImage" data-dismiss="modal">Close</button>
            <button type="button" class="btn btn-primary btn-sm">Save changes</button>
          </div>
        </div>
      </div>



    <script type="text/javascript" src="/js/jquery-3.4.1.min.js"></script>
    <script src="/js/login.js"></script>
  </body>
</html>
