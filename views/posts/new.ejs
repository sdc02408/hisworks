<html>
<head>
    <%- include('../partials/head') %>

    <link href="/css/writeboard.css" rel="stylesheet">
    <link href="/css/common.css" rel="stylesheet">
</head>
<body>
<%- include('../partials/mynav') %>

<div class="container ">

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mt-3 p-1 pl-2 pr-2">
            <li class="breadcrumb-item"><a href="/">홈</a></li>
            <li class="breadcrumb-item"><a href="/posts">게시판</a></li>
            <li class="breadcrumb-item active" aria-current="page">글쓰기</li>
        </ol>
    </nav>

    <form action="/posts<%= getPostQueryString() %>" method="post">

        <div class="form-group">
            <label for="title">제목</label>
            <input type="text" id="title" name="title" value="<%= post.title %>"
                   class="board-title <%= (errors.title) ? 'is-invalid' : '' %>">
            <% if(errors.title){ %>
                <span class="invalid-feedback"><%= errors.title.message %></span>
            <% } %>
        </div>

        <div class="form-group">
            <label for="body">내용</label>
            <textarea id="body" name="body" rows="5"
                      class="board-content <%= (errors.body) ? 'is-invalid' : '' %>"><%= post.body %></textarea>
            <% if(errors.body){ %>
                <span class="invalid-feedback"><%= errors.body.message %></span>
            <% } %>
        </div>

        <% if(errors.unhandled){ %>
            <div class="invalid-feedback d-block">
                <%= errors.unhandled %>
            </div>
        <% } %>

        <div>
            <a class="btn mybtn" href="/posts<%= getPostQueryString() %>">돌아가기</a>
            <button type="submit" class="btn mybtn">제출하기</button>
        </div>

    </form>

</div>

</body>
</html>
